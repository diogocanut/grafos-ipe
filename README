 A escolha da implementação foi a lista de adjacência, devido a necessidade de armazenar informações nos vertices, como: nome, numero, latitude, longitude. E nas arestas o peso. Para isso foi utilizado uma strutura para representar os vertices, que possui um ponteiro para a estrutura das arestas. Dessa forma obtemos um vetor de vertices e suas respectivas arestas.

 Para inicializar o codigo, defina o diretório nas funções de leitura de arestas e vertices, em seguida compile o código e acompanhe o menu iterativo.
